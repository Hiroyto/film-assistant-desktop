// BC-04 Scenes Canvas — 2ª superfície UI (Segment Canvas + Scenes Canvas) do MESMO
// aggregate Scene. Compartilha o model de `features/scenes` (um bounded context).
// O `components/canvas/` legado é fundido aqui (UI preservada — Op3).
export * from '../scenes/model/sceneOps';
export * from '../scenes/model/saveScenes';
